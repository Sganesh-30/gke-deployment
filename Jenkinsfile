pipeline {
    agent any 

    environment {
        PROJECT_ID =
        IMAGE_NAME =
        IMAGE_TAG =
        REGION =
        CLUSTER =
        CLUSTER_ZONE =
    }

    stages {
        stage(' ')
    }
}